# プロジェクト仕様書

## 概要

GitHub上のリポジトリにホストされたObsidian vault（Markdownノート群）を、そのままブラウザで閲覧できる read-only Webビューア。
フォルダ階層、Obsidian独自の `[[wikilink]]` 相互リンク、`![[embed]]` 埋め込み、画像・添付ファイルを、GitHub REST APIから直接取得して表示する。
複数ノートのタブ表示・ペイン分割、高度な検索（ファイル名・本文・タグ）、閲覧アナリティクス（Supabase連携）にも対応する。
今回のスコープは**閲覧機能のみ**。編集・保存（書き込み）機能は実装しないが、後続フェーズで追加しやすいよう、GitHub APIとのやり取りは
UIコンポーネントから分離したデータ層（hooks/stores）に集約すること。

## 技術スタック

- React 19 (TypeScript)
- Vite
- TailwindCSS v3+
- React Router v6+
- Zustand（状態管理）
- react-resizable-panels（ペイン分割）
- @supabase/supabase-js（アナリティクス永続化、オプション）
- nanoid（一意ID生成）
- デプロイ先: GitHub Pages
- 追加ライブラリ（提案。既存の規約に反しない範囲で選定してよい）
  - Markdown解析: `unified` + `remark-parse` + `remark-gfm`（表・タスクリスト等のGFM対応）
  - フロントマター解析: `gray-matter`
  - wikilink解決: 既存のremarkプラグイン（例: `remark-wiki-link` 系）を評価し、Obsidianのエイリアス記法 `[[Note|Alias]]` に対応できない場合は自前のremarkプラグインを実装する
  - コードブロックのシンタックスハイライト: 任意（`rehype-highlight` など。最初のフェーズでは必須ではない）

## 機能要件

### 基本閲覧機能

- [x] **接続設定画面**: owner / repo / branch を入力してvaultを開く。プライベートリポジトリやAPIレート制限緩和のため、Personal Access Token（fine-grained, Contents: Read only）を任意入力できるフィールドを設ける。トークンはブラウザのメモリ上（Zustandストア）にのみ保持し、永続化・送信はしない。
- [x] **ファイルツリー取得**: `GET /repos/{owner}/{repo}/git/trees/{branch}?recursive=1` を一度だけ呼び出し、リポジトリ全体のファイルパス一覧を取得する。
- [x] **フォルダ階層表示**: 取得したフラットなpath一覧をネスト構造に変換し、折りたたみ可能なツリー形式のサイドバーに表示する。ファイル種別（`.md` / 画像 / その他）ごとにアイコンを出し分ける。
- [x] **ノート表示**: 選択した `.md` ファイルをGitHub Contents API (`GET /repos/{owner}/{repo}/contents/{path}?ref={branch}`) から取得し、Markdownとしてレンダリングする。
  - GFM（表、打消し線、タスクリストなど）に対応する
  - YAMLフロントマターを解析し、本文からは除去したうえでプロパティパネルとして別枠表示する
- [x] **wikilinkの解決とレンダリング**: `[[ノート名]]` および `[[ノート名|表示名]]` を、vault内の対応ファイルへの内部リンク（React Router経由の画面遷移）に変換する。
  - ファイルベース名で照合し、拡張子省略・大文字小文字の違いを許容する
  - `[[folder/ノート名]]` のようなパス付き指定にも対応する
  - 該当ファイルが存在しない場合は「未作成リンク」として視覚的に区別（破線表示など）し、通常のノートへのリンクと混同しないようにする
  - 同名ファイルが複数ある場合の挙動（例: 最も浅い階層を優先）を実装コメントとして明記し、TODOではなく明示的なルールとして扱う
  - `[[Note#見出し]]` や `[[Note#^blockid]]` のような見出し・ブロック参照は、今回はNoteへのリンクとして扱うだけでよい（見出しへのスクロールは対象外、コード中にTODOコメントを残す）
- [x] **embed（埋め込み）のレンダリング**:
  - `![[ノート名]]` → 該当ノートの内容をその場に埋め込み表示する。埋め込みの中でさらに埋め込みが発生する場合に無限ループしないよう、再帰の深さ制限（例: 1階層まで）または訪問済みノートの記録を実装する
  - `![[画像ファイル名]]` → 画像として埋め込み表示する
  - `![[画像ファイル名|幅]]` のようなサイズ指定（Obsidianの記法）があれば、width指定として反映する（余力があれば）
- [x] **画像・添付ファイルの表示**: 標準Markdown記法 `![alt](path)` とObsidianのembed記法の両方から画像を表示できるようにする。GitHub Contents APIのbase64レスポンス、または `raw.githubusercontent.com` の生URLのどちらを使うか設計判断として明記する。画像以外の添付（PDFなど）はダウンロードリンク＋ファイル種別アイコン表示のみでよい。
- [x] **タグ表示**: フロントマターの `tags` と本文中の `#tag` 記法を検出し、ノート上部・下部にタグの一覧を表示する。
- [x] **ローディング/エラー状態**: API呼び出し中はスケルトン表示、レート制限超過・ファイル未検出・ネットワークエラー時にはそれぞれ状況に応じたメッセージを表示する。
- [x] **将来の編集機能に備えたデータ層分離**: GitHub APIとの通信は `useVaultRepo` / `useNoteContent` のようなカスタムフックまたはZustandストアに集約し、UIコンポーネントが直接 `fetch` を呼ばない構成にする。これにより後続フェーズで保存（`PUT /contents/{path}`）機能を同じ層に追加できるようにする。

### 複数タブ + ペイン分割

- [x] **タブ管理**: サイドバーからファイルをクリックするとタブで開く。既に開いているファイルは再フォーカスする。
- [x] **タブ操作**: 閉じる（×ボタン / 中クリック）、他のタブをすべて閉じる。
- [x] **ペイン分割**: タブ右クリック →「右に分割」「下に分割」で上下左右にペインを分割。各ペインは独立したタブバーを持つ。
- [x] **ペインリサイズ**: ペイン間のセパレーターをドラッグしてサイズ調整可能。
- [x] **ペインツリー**: `tabStore` で再帰的なペインツリーを管理。`LeafPane`（タブを持つ末端ペイン）と `SplitPane`（分割コンテナ）で構成。

### 検索機能の強化

- [x] **ファイル名検索**: サイドバー上部のインクリメンタル検索ボックスで、パス・ファイル名でフィルタする。
- [x] **本文検索**: ノートの内容をフルテキスト検索し、マッチ箇所のスニペット（前後のコンテキスト付き）をプレビュー表示する。検索クエリのマッチ箇所をハイライトする。
- [x] **検索モード切替**: 検索窓のアイコンクリックで「ファイル名検索」「本文検索」「タグ検索」の3モードを切り替えられるUIを表示する。

### タグ検索機能

- [x] **タグインデックス**: Vault 内のすべてのノートからタグ（フロントマター `tags` + 本文中 `#tag`）を抽出し、タグ名 → ファイルパス集合のインデックスを構築する。
- [x] **タグ一覧表示**: 検索モードで「タグ検索」を選択すると、存在するタグをアルファベット順にバッジ形式で一覧表示する。
- [x] **タグ選択による絞り込み**: タグバッジをクリックすると、そのタグを含むノートの一覧がサイドバーに表示される。
- [x] **タグクリック連動**: ノート最上部・最下部の `TagList` およびフロントマターの `tags` フィールドのバッジをクリックすると、サイドバーのタグ検索モードが起動し、該当タグで絞り込む。

### 閲覧アナリティクス（Supabase連携、オプション）

- [x] **閲覧回数記録**: ノート表示時に Supabase の `note_views` テーブルに閲覧回数を upsert する。Supabase 未設定時はメモリ内フォールバックで動作する。
- [x] **人気ノートランキング**: サイドバー下部のアナリティクスダッシュボードに、閲覧回数上位10件のノートをランキング表示する。
- [x] **ヒートマップ**: Vault 全体のノートを閲覧頻度に応じて色分け（青→黄→赤のグラデーション）した「地図」表示。
- [x] **フォルダフィルタ**: ヒートマップの表示対象をフォルダパスで絞り込み可能。AND（すべてのフォルダに含まれる）/ OR（いずれかのフォルダに含まれる）演算子を選択可能。
- [x] **フィルタ永続化**: フィルタ設定は Supabase の `analytics_filters` テーブルに保存され、セッション間で維持される。
- [x] **環境変数管理**: Supabase 接続情報（`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`）は `.env` ファイルで管理し、`.gitignore` でGit管理外とする。

### 今回のスコープ外（実装しない）

- ノートの編集・保存（書き込み）機能
- OAuthによるログインフロー
- 複数人での同時編集・競合解決
- Obsidianのコミュニティプラグイン相当の機能（グラフビュー、Dataviewなど）

## 非機能要件

- レスポンシブデザイン（モバイルファースト）
- ダークモード対応
- Lighthouse パフォーマンススコア 90 以上を目標
- アクセシビリティ（WCAG 2.1 AA 準拠を目標）
- GitHub APIのレート制限（未認証60回/時、認証済み5,000回/時）を考慮し、同一セッション内で同じファイルを再取得しないようクライアント側でキャッシュする

## ページ構成

| ページ名      | パス       | 概要                                                                                                                                                  |
| ------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 接続設定      | `/`        | owner / repo / branch（および任意でPAT）を入力してvaultを開く画面                                                                                     |
| Vaultビューア | `/vault/*` | サイドバー（フォルダツリー・検索・タグ検索・アナリティクス）＋メインペイン（タブ＋ペイン分割）の構成。パス部分でどのノートを開いているかディープリンクできるようにする |

## デザイン方針

- 長文のMarkdownノートを読むことが主目的なので、装飾よりも可読性を優先する（適切な行長、行間、余白）
- サイドバーのツリーとメインの読書ペインという2ペイン構成を軸にする
- ダークモード・グラスモーフィズム・グラデーション等はテンプレート標準の範囲で適用し、本文エリアは過度な装飾を避ける
- カラーパレットは `tailwind.config.ts` で一元管理する

## API 仕様

### GitHub API

| メソッド | エンドポイント                                                               | 説明                                                         |
| -------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------ |
| GET      | `https://api.github.com/repos/{owner}/{repo}/git/trees/{branch}?recursive=1` | vault内の全ファイルパス一覧を取得（`.md`・画像・添付を含む） |
| GET      | `https://api.github.com/repos/{owner}/{repo}/contents/{path}?ref={branch}`   | 個別ファイル（ノート・画像・添付）の内容取得（base64）       |

- 認証: PATが入力されている場合のみ `Authorization: Bearer {token}` ヘッダーを付与する。未入力時は認証なしでリクエストし、公開リポジトリのみ閲覧可能とする。

### Supabase API（オプション）

| テーブル名 | 説明 |
|-----------|------|
| `note_views` | ノートごとの閲覧回数（`repo_key`, `file_path`, `view_count`, `last_viewed_at`） |
| `analytics_filters` | アナリティクスフィルタ設定（`repo_key`, `filter_name`, `folder_paths`, `operator`） |

- `@supabase/supabase-js` を使用し、`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` で接続する。
- 環境変数が未設定の場合、アナリティクス機能は自動的に無効になる（グレースフルデグレード）。

## ストア設計

| ストア名 | 責務 |
|---------|------|
| `vaultStore` | Vault 接続情報、ファイルツリー、ファイルコンテンツキャッシュ |
| `themeStore` | ダークモード切り替え |
| `searchStore` | 検索モード（filename / content / tag）、検索クエリ、選択タグ、フォーカス状態 |
| `tabStore` | タブ管理、ペインツリー（再帰的 LeafPane / SplitPane 構造） |
| `analyticsStore` | 閲覧回数データ、ヒートマップ生成、フィルタ管理 |

## 更新履歴

| 日付       | 版  | 内容                                 |
| ---------- | --- | ------------------------------------ |
| 2026-09-03 | 1.0 | 初版作成（読み取り専用ビューア仕様） |
| 2026-09-10 | 2.0 | 複数タブ＋ペイン分割、検索強化、タグ検索、アナリティクス機能を追加 |
